/* AnnouncementBar is currently disabled while we have nothing live to
   announce. Re-enable by removing the early return and updating the copy. */
export default function AnnouncementBar() {
  return null;
}
